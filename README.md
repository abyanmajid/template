# Template

## Adding components

To add components to your app, run the following command at the root of your `a3n-web` app:

```bash
pnpm dlx shadcn@latest add button -c apps/a3n-web
```

This will place the ui components in the `packages/ui/src/components` directory.